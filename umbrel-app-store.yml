# Umbrel Community App Store manifest.
# Add this store to Umbrel via: Settings → App Store → Community App Stores →
# paste this repo's URL. `id` must be globally unique & lowercase.
id: floortrades
name: FloorTrades
